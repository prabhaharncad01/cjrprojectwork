# cjrprojectwork
To implement the K-Means clustering algorithm from fundamental principles using only NumPy, generate a synthetic dataset with known clusters, evaluate clustering performance using Inertia (SSE) and Silhouette Score, and visualize the final clustering results.
